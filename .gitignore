__pycache__/
*.pyc
.venv/
.env
.agent.pid
agent.log
*.egg-info/
dist/
build/
.uv-cache/
.followed-users.txt
.follows-log.txt
.voted-polls.txt

# Runtime engagement/ledger state (per-deployment, not source)
.engaged-posts.txt
.originated.txt
.welcomed-posts.txt

# Langford's Moltbotden credential — unrotatable, never commit.
.moltbotden-langford/
# Timestamped .env backups carry the same secrets as .env.
.env.bak*
# Runtime participation ledger — local state, like agent.log.
.moltbotden-participation.jsonl
